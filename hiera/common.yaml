# See parameter documentation inside ../manifests/single_node_ci.pp
# Fields commented out have reasonable default values

#vhost_name:
project_config_repo:

gearman_server: 172.130.0.141
#serveradmin:
jenkins_username:
jenkins_password:
jenkins_ssh_private_key: |
                         -----BEGIN RSA PRIVATE KEY-----
                         -----END RSA PRIVATE KEY-----
jenkins_ssh_public_key: # only fill with the body of public key
gerrit_server:
#gerrit_ssh_host_key:
gerrit_user:
gerrit_user_ssh_private_key: |
                             -----BEGIN RSA PRIVATE KEY-----
                             -----END RSA PRIVATE KEY-----
gerrit_user_ssh_public_key: # only fill with the body of public key
git_email:
git_name:
log_server: # opnfv.zte.com.cn
#smtp_host:
#smtp_default_from:
#smtp_default_to:
zuul_revision:
zuul_git_source_repo: # replace with local repo
oscc_file_contents: |
                    # Do Not Edit - Generated & Managed by Puppet
                    #
                    # Insert OSCC file contents here as explained in the
                    # documentation so that nodepool is able to
                    # authenticate to your cloud(s)
                    client:
                      force_ipv4: true
                    clouds:
                      zte-pod13:
                        profile: internap
                        floating_ip_source: 'neutron'
                        auth:
                          username: 'ciuser'
                          password: 'ciuser'
                          project_name: 'ciuser'
                          auth_url: 'http://h13.oz:5000/v2.0'
                        regions:
                          - name: RegionOne
                            values:
                              networks:
                              - name: admin_floating_net
                                routes_externally: true
                              - name: net-ci
                                nat_destination: true
                                default_interface: true
                      zte-pod11:
                        profile: internap
                        floating_ip_source: 'neutron'
                        auth:
                          username: 'ciuser'
                          password: 'ciuser'
                          project_name: 'ciuser'
                          auth_url: 'http://h11.oz:5000/v2.0'
                        regions:
                          - name: RegionOne
                            values:
                              networks:
                              - name: admin_floating_net
                                routes_externally: true
                              - name: net-ci
                                nat_destination: true
                                default_interface: true
#nodepool mysql db config:
mysql_bind_address: '0.0.0.0'
mysql_root_password: root123
mysql_nodepool_password: root123

#gerrit mysql db config:
mysql_gerrit_root_password: UNSET
mysql_gerrit_name: reviewdb
mysql_gerrit_user: gerrit2
mysql_gerrit_password: 12345

nodepool_jenkins_target:
jenkins_api_key: # jenkins api key get from Jenkins
jenkins_credentials_id: # jenkins api key get from Jenkins
nodepool_revision:
nodepool_git_source_repo: # replace with local repo
